services:
  - type: web
    name: stayease
    env: python
    pythonVersion: 3.12
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn stayease.wsgi